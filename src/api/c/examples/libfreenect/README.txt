rungl, runcamtest, and runchunkview do not work everything else works though

run the following commands first
	make 
	make runfakenect
	make (whatever target you want)

the targets all run their functionality with the data that gets stored in ./sessions.


